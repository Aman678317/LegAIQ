/**
 * Intentionally empty.
 *
 * The user journey is covered by auth.spec.ts + case-journey.spec.ts against
 * the page-route mock layer in mocks.ts. (An earlier mock-server-based
 * variant lived here; it was removed in favour of the route-mock approach.)
 */
export {};
